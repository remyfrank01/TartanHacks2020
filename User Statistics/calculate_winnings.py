# given a dictionary of data and key of userID and returns a 
#list of tuples which are (time, money) for total money after each bet

def calculate_winnings(userID, database):
    result = lst[]
    bets = lst of bets
    for i in range(len(bets)):
        result.append((i,bets[i]))
    return result